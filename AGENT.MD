# Quant Skills Repository Guide

本仓库用于维护可复用的量化研究 Skills。所有修改都应优先保证研究口径严谨、结果可复现、说明简洁，并遵循渐进式披露：核心流程写入 `SKILL.md`，详细定义按需放入 `references/`，稳定且重复执行的逻辑才沉淀为 `scripts/`。

## 目录结构

```text
quant-skills/
├── factor-research/
│   ├── SKILL.md
│   ├── references/
│   └── scripts/
├── strategy-backtest/
│   ├── SKILL.md
│   ├── references/
│   └── scripts/
├── portfolio-analysis/
│   ├── SKILL.md
│   └── references/
└── market-research/
    └── SKILL.md
```

不要为了保持目录对称而创建空目录。只有实际需要时才添加以下资源：

- `SKILL.md`：每个 Skill 必需，包含触发描述、核心工作流和资源导航。
- `references/`：存放指标定义、数据字段、研究规范、方法说明等按需加载的资料。
- `scripts/`：仅存放需要反复执行、对确定性或稳定性有明确要求的代码。
- `assets/`：仅在需要报告模板、配置模板或其他输出素材时添加。

## SKILL.md 规范

每个 `SKILL.md` 必须以 YAML frontmatter 开头，并且只包含：

```yaml
---
name: skill-name
description: 清楚说明该 Skill 能做什么，以及应在什么请求或场景下触发。
---
```

编写时遵循以下要求：

- 目录名与 `name` 完全一致，只使用小写字母、数字和连字符。
- `description` 同时覆盖能力和触发条件；不要在正文中另设“何时使用”章节。
- 正文使用指令式表达，保留完成任务所需的核心步骤、判断规则和失败处理。
- 保持正文精简，原则上不超过 500 行；详细口径、字段表和长示例拆入 `references/`。
- 从 `SKILL.md` 直接链接所需资源，并说明何时读取或执行；避免多层嵌套引用。
- 同一信息只保留一份，不在正文与引用资料之间重复维护。
- 不添加与 Skill 执行无关的 `README.md`、安装指南、变更日志等辅助文档。

## 量化研究通用纪律

所有研究、回测和分析必须显式处理适用的下列问题：

- 明确研究对象、市场、频率、样本区间、基准、时区、交易日历和数据来源。
- 记录字段含义、单位、币种、复权方式、缺失值规则和异常值处理。
- 防止前视偏差、幸存者偏差和数据泄漏；因子值、成分股及财务数据必须按当时可获得时间对齐。
- 区分信号生成、订单生成和成交时间，禁止默认使用尚不可得的收盘价成交。
- 回测应纳入适用的手续费、税费、滑点、冲击成本、最小交易单位、涨跌停、停牌、流动性及容量约束。
- 明确调仓频率、权重约束、现金处理、分红拆并股及退市证券处理规则。
- 区分样本内、验证集和样本外结果；参数选择不得借用样本外信息。
- 同时报告收益与风险，不以单一指标下结论；必要时包含基准对比、回撤、换手、暴露和稳健性检验。
- 对无法验证的数据、假设或结论明确标注，不虚构行情、字段、来源或绩效。
- 研究输出不构成投资建议；避免把历史回测表述为未来收益承诺。

## References 规范

- 一个文件聚焦一个主题，使用可检索、稳定的文件名。
- 指标定义至少说明公式、输入字段、频率、方向、边界条件和缺失值处理。
- 数据字典至少说明字段名、类型、单位、时区、复权口径和可获得时间。
- 研究规范应写清假设、适用范围和已知限制。
- 超过 100 行的文件在顶部添加目录；较长资料应在对应 `SKILL.md` 中给出建议检索词。

## Scripts 规范

- 优先提供参数化命令行接口，避免硬编码本机路径、密钥、账户或数据日期。
- 固定随机种子，并记录会影响结果的参数、依赖版本和运行环境。
- 输入校验应尽早失败，错误信息应说明问题字段和修复方向。
- 输出应稳定、可追溯；禁止静默覆盖重要结果，除非接口明确要求。
- 数值计算应避免隐式类型转换，并妥善处理 NaN、无穷值、零除和浮点误差。
- 新增或修改脚本后必须实际运行；至少覆盖正常路径和一个关键边界条件。
- 生成物与临时文件不要提交，除非它们是明确需要版本管理的基准样例或资产。

## 工作流程

1. 先确认目标 Skill 的边界、典型触发请求和预期输出。
2. 判断内容属于核心流程、按需参考、确定性脚本还是输出资产。
3. 优先修改已有文件，只有明确需要时才新增目录或资源。
4. 修改 Skill 后检查 frontmatter、链接、命令、示例和资源路径。
5. 修改脚本后运行测试或代表性样例，并核对关键数值与边界情况。
6. 提交前检查是否引入未来函数、错误数据对齐、成本遗漏或不可复现步骤。

## 完成标准

- Skill 能被 `description` 准确触发，正文可以独立指导代理完成目标任务。
- 所有引用资源都可发现、必要且没有重复内容。
- 所有脚本已实际执行验证，结果在相同输入与环境下可复现。
- 所有量化结论都能追溯到数据、假设、公式或计算步骤。
- 未创建空目录、占位文件、无关文档或未经说明的生成物。
